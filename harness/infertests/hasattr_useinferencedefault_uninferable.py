import sys

class D:
    pass

def f(self):
    if hasattr(self.device.state, "active_time"):
        pass
    if hasattr(sys.modules.get("airflow.x"), "SUPervisor"):
        pass
    if hasattr(undefined_thing, "x"):
        pass
    if hasattr(D, "x"):
        pass
    if hasattr(D(), 5):
        pass
